const title = document.getElementById("title");
title.innerHTML = "Hi! From JS"; //innerHTML can changes HTML body's contents

console.log(title);

//JS makes DOM(Document Object Module) object using all kind of html tag
console.error("ERR"); //print error log